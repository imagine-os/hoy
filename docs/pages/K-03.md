---
title: K-03 — Operations manual
code: K-03
route: /#/manual
roles: super_admin, admin, coordinator, front_desk, finance, teacher, maintenance, customer, public
status: built
---

# K-03 — Operations manual

**Route** `/#/manual` · `/#/manual/:chapter` · **Roles** super_admin, admin, coordinator, front_desk, finance, teacher, maintenance, customer, public · **Surface** docs · **Spec** `spec.code === 'K-03'` (see `/#/dev/specs`)

## Purpose
How the club runs in person and in software, by role. Same for everyone who looks; Spanish first, English as a mirror.

## Screenshots
| ES · mobile | EN · mobile |
| --- | --- |
| ![K-03 es 390](../screenshots/K-03/es-390.jpg) | ![K-03 en 390](../screenshots/K-03/en-390.jpg) |

| ES · desktop | EN · desktop |
| --- | --- |
| ![K-03 es 1280](../screenshots/K-03/es-1280.jpg) | ![K-03 en 1280](../screenshots/K-03/en-1280.jpg) |

| ES · dark | EN · dark |
| --- | --- |
| ![K-03 es 1280 dark](../screenshots/K-03/es-1280-dark.jpg) | ![K-03 en 1280 dark](../screenshots/K-03/en-1280-dark.jpg) |

## Sections (layout order — `useLayout(spec)`)
1. `ChapterSidebar (titles from front matter, decisions summary link, print)`
2. `ChapterMeta (role, version, updated)`
3. `MarkdownViewer (callouts, screenshot placeholders)`
4. `PrevNext`

## Data
| Table | Read / write | Notes |
| --- | --- | --- |
| `docs_entries` | read | |

## Logic and integrations
- Chapters are docs/ops-manual/<lang>/<NN-slug>.md loaded with import.meta.glob at build time; the slug is shared across languages.
- Follows the app language: ES is the source; a missing EN chapter falls back to ES with a notice.
- `> DECISIÓN PENDIENTE:` / `> DECISION NEEDED:` blockquotes render as highlighted callouts and feed K-04.
- `[screenshot: CODE — caption]` lines render as dashed placeholder boxes until a real capture replaces them.
- Print: sidebar and shell chrome hidden; callouts and placeholders never split across pages.
- Integrations: none

## States
- default
- chapter not found
- untranslated (ES fallback)
- print

## Real vs mock
- Real: the page renders from the data layer (`useData()` / `useTable`) over the tables above; every write goes through the `DataProvider` so the Supabase provider replaces the mock unchanged.
- Mock / pending: no external integrations; data is the browser-local `MockProvider` seed.
- Note: Policies quoted in the manual (cancellation 2 h, waitlist 30 min, late grace) are read from M-08; the screen wins over the text.

## Changelog
- `docs/changelog/0005-final-integration.md` — screenshots and this page doc (v0.3.0)

---
**Resumen (ES).** Manual de operaciones. Cómo funciona el club en persona y en el software, por rol. Igual para todos los que lo miran; español primero, inglés como espejo.
