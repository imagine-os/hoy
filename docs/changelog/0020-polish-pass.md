version: 0.7.1
date: 2026-09-17
prompt: docs/prompts/0020-polish-pass.md
intent: Justin: "please go through and make sure everything is polished and cleanly coded and stuff". Run the code-quality, docs and visual audits over the whole system and apply every fix that needs no product decision, as one version with no new feature.
decision: **Six concerns, one version.** (1) **Route-level code splitting that keeps the registry contract.** Every module's `index.ts` still exports `{ routes, strings }` synchronously, but its page components moved to a `pages.ts` barrel that `src/app/lazyPage.ts` loads on first visit (`React.lazy`; one `<Suspense>` around `<Routes>` in `App.tsx`, fallback `.lazy-fallback`). Rollup dedupes the dynamic import, so each surface is one chunk. The markdown under `docs/` stopped being inlined: `scripts/lib/docmeta.mjs` is a Vite plugin that serves `*.md?docmeta` (title, header meta, headings, decisions, placeholders, word and figure counts) at build time, `docsIndex.ts` keeps the eager index over that and fetches each body as its own `?raw` chunk (`useDocSource`, `useDocSources`, `loadDoc`), and `manualIndex.ts` derives every `Chapter` field from the same metadata with `useChapterBody()` for the page and `loadBodies()` for search. Main chunk **2 549 kB → 796 kB** (gzip 788 → 259 kB), 235 chunks, `dist/` 65 MB because the screenshots are still shipped (§F 31). (2) **The date-key bug.** `src/i18n/format.ts` gained `dateKey()` (local `YYYY-MM-DD`), `fromDateKey()` (local noon), `addDays` / `addMonths` / `startOfDay` / `addDaysKey`, `MS = { min, hour, day }`, `localeOf`, `digitsOf` / `parseDigits` / `isPhone` and `waLink`; `formatDate` / `formatTime` / `formatDateTime` accept a Date or a bare date key (read as local noon). All 14 `toISOString().slice(0, 10)` sites (credit expiry, intention of the day, gift date, membership dates, invoice file names, seed ids) and 11 `T12:00:00` workarounds were replaced; `payrollCalc.local()` and the seed's `dateOnly` are gone. `scripts/test-date-keys.mjs` (`npm run test:dates`, no dependency) sets `TZ=America/Bogota` and proves that 20:00 local still names today while the ISO string already says tomorrow. (3) **Dead code and duplicates.** Deleted: `getModules`, `roleFromCanvasLabel`, `sumAmount`, `periodContaining`, `byTier`, `toggleInspector`, `useBi`, `periodKey`, `currentPolicy`, `PART_KEYS`, `BOOKING_STATUSES`, `brandContent`, `type Tenant`, the three unused barrels (`auth/index.ts`, `data/index.ts`, `i18n/index.ts`), the orphan `.adm-line-manual .grow` rule, 40 unused string keys (12 core, 21 admin incl. the twelve `admin.settings.int.*` superseded by M-10, 2 customer, 2 dev, 1 staff, 2 website) and the 7 `eslint-disable` comments for a linter the repo does not have; 25 single-file symbols un-exported. One implementation each: `formatCOP` (LiveBlock's `cop`, BarList's meta and TablesPage's `toLocaleString` deleted), `splitIva` in the new `src/data/tax.ts` with `DEFAULT_IVA_PCT` (OrderSummary, `settings.splitTax`, `recordPayment`, the two seed `1.19` literals), `waLink` (customer `ui.tsx` and `settings.waLinkFor` deleted; InvitePage's raw wa.me string gone), `fakeRef` for the simulated Wompi references, `MS` for the 47 millisecond literals. `tsconfig.json` enables `noImplicitReturns` and `noImplicitOverride` (0 errors). (4) **Strings.** Every shared component reads through `useT()`: TopBar (`core.nav.back`), NavBar (`core.nav.main`), LangToggle (`core.lang.toggle`), ClassRow / RosterRow (`core.status.*`), Receipt (`core.common.subtotal` / `total`), PageStub (`core.stub.layout`), `DesktopShell` takes a `titleKey` (shells pass `core.nav.group.*`), SiteShell, TablesPage and SettingsPage aria-labels, the Sandbox option, EmailsPage footer and template names as `Bi`, WhatsAppPage delay / category / status, RegisterPage subtotal, ManualPage meta labels (with a singular `manual.meta.figure`), `PAYMENT_METHODS.label` as `Bi`. `LiveBlock` was rewritten on ~100 `manual.live.*` keys (its ~60 `lang === 'en' ? … : …` ternaries are gone) and types its policy record instead of `as unknown as`. New tables: `admin.flag.*` — the 160 spec toggle labels in Spanish, read by M-08b through the label slug the seed already uses as `feature_flags.key`; `admin.audit.*` — 60 action titles read by `auditTitle()` in `staff/audit.ts` for the M-01 / S-01 feeds (a new action falls back to its key); `admin.finance.status.*` / `.provider.*` / `.product.special`. (5) **Tokens and tenant facts.** 4 hex, 6 literal rgba, 22 raw `font-size` px, 8 raw radii and 5 inline styles became `--fs-*`, `--r-*`, `rgba(var(--m-*), α)` and classes; D-01 gained `--r-2xs` (2 px hairline radius) and `--color-text-faint` moved from .55 to .72 in both themes so 11 px eyebrows pass 4.5:1 on cards. `tenant.ts` gained `dialCode` (`+57`, read by every phone default and placeholder), `invoicePrefix` (`HOY-…` numbers, invite and gift codes), a structured `openingHours` from which `hoursSentence()` derives the ES / EN hours line (`DEFAULT_SETTINGS.openingHours` now reads it — one source), and a neutral address placeholder (`El Poblado`) so the footer and the legal documents no longer print "(pendiente) (pending)". Email subjects use `{{studio}}`, deep links use `${tenant.slug}app://`, the manual's Parte I label is `tenant.name`, C-05 reads `settings.payments.bankName`. (6) **The visual audit.** `formatCOP` uses `currencyDisplay: 'narrowSymbol'` so both languages read `$` and the M-09 tiles fit; payment statuses, products (`Specials (no plan)` instead of `—`), providers and plan names are bilingual on M-09 and S-02; `Card` headers wrap (`.card-head` / `.card-actions` flex-wrap) which fixes the M-09 one-letter-per-line eyebrow at 390 and the S-04 horizontal overflow; chips inside a `role="tablist"` row are `nowrap` (a sentence chip such as the S-03 note still wraps) and every chip row wraps; M-09c status cells keep chip + actions on one line (`.adm-status-actions`); `.cust-plans` is `auto-fit` so one plan fills the row; the C-02 filter strip and the S-02 class strip fade at the right edge (`mask-image`); `RoomDayGrid` blocks under an hour drop their sub-line and under ~35 min keep the title only (`is-short` / `is-tiny`); the S-05 upcoming list puts the date on its own line; `DesktopShell` lights only the most specific nav item and titles the top bar with the nav label; the `NotificationBell` renders only when the role may open `/admin/whatsapp`; M-01's check-in link, S-01's quick actions and HUB-01's dev card are gated by `can('checkin.write')` / `isSuperAdmin`; W-01 says "today's classes are done" (`site.today.done`) and S-01 lists the day's taught classes instead of repeating "no classes left"; K-04 strips the markdown marks from decision lines; `EmptyState`'s default glyph is `○`; the seed rolls `renews_at` forward so an active membership never shows a past renewal; the P-01 monthly-equivalent figure is computed from the annual price; `RouteTitle` sets `document.title` to "<page> · HOY". `scripts/screenshots.mjs` now signs in as each surface's demo user (a route whose roles exclude it falls back to the first allowed role), reads real ids from the seeded mock DB for the seven param routes, saves the manual cover as `-cover.jpg`, waits for lazy chunks, and the manifest carries each route's `roles` plus the demo users. (7) **Docs.** `scripts/gen-flow-map.mjs` (`npm run flow-map`) regenerates the route tables of `docs/flow-map.md` from `routes.json` (96 routes, 84 codes, 10 sections; the Dependencies section stays hand-written); `docs/prompts/0009-…` and `0013-…` reconstructed and marked as such; `codes:` headers on 0001 / 0004; manual index / README versions and the 72-figure count; M-08 family index lists M-08f; K-03 and C-07b page docs corrected; ROADMAP §A (0.7.1, the missing v0.5.1 bullet, every count: 48 tables of which 20 with `rls`, 391 captures, 56 components, 72 figures, 36 decisions, 12 media slots) and §F 25–35; README and `docs/README.md` maps; kanban Backlog cleaned of done cards.
rejected: (1) Keeping the markdown eager inside the lazy docs chunk (no plugin) — simplest, but every visit to `/manual` would still fetch 1.1 MB of text for one chapter; the `?docmeta` plugin is 60 lines with no dependency and makes a chapter cost its own body plus a 260 kB index. (2) A generated `docs/index.json` written by a prebuild script — one more artefact to keep in sync and `npm run dev` would need it too; the plugin runs in dev with `addWatchFile`. (3) Lazy-loading only docs, ops-manual and dev as first proposed — measured at 1 394 kB, still above the 1 MB target because `react-markdown` and the customer / admin surfaces stayed in the main chunk; one `pages.ts` per module was the same mechanism applied nine times. (4) A `Date`-parsing library (date-fns, dayjs) — a dependency for six one-line helpers; the bug is naming, not arithmetic. (5) Fixing the date bug by setting the seed / server clock to UTC−5 — the browser already runs in the studio's zone; the fault was `toISOString()`. (6) Separate `subtotalLabel` / `totalLabel` props on `Receipt` — two `core.common.*` keys give the same result with no caller change. (7) Making `email_templates.name` a jsonb `Bi` — a schema change for seed content that is shown as-is in one admin list; the defaults are `Bi` and the row keeps Spanish (bilingual seed names are §F 32). (8) Deleting the `core.theme.light` key as a "toggle bug" — the TokensPage switch is labelled "Oscuro" because it *is* the dark-mode switch; the key was simply unused. (9) Adding eslint / prettier in this pass to make the 7 `eslint-disable` comments real — a dependency decision with its own changelog entry (§F 30); the comments were removed instead. (10) Rendering the M-08b switch labels from a `Bi` in `canvasSpecs.ts` — that file is generated from the canvas; a string table keyed by the label slug leaves the generator alone. (11) Formatting the M-08c rate-card inputs on blur — an uncontrolled-value dance for a hint; a formatted `$` beside each input says the same. (12) Hiding the HUB-01 dev card for every non-super role but keeping it for the visitor — the visitor is exactly who bounced six times to `/no-access`.
files: vite.config.ts tsconfig.json package.json scripts/lib/docmeta.mjs scripts/gen-flow-map.mjs scripts/test-date-keys.mjs scripts/screenshots.mjs src/app/{App.tsx,lazyPage.ts,RouteTitle.tsx,manifest.ts,registry.ts,shells.tsx} src/modules/*/pages.ts src/modules/*/index.ts src/modules/docs/{docsIndex.ts,DocsBrowser.tsx,views.tsx,specs.ts} src/modules/ops-manual/{manualIndex.ts,ManualPage.tsx,DecisionsPage.tsx,strings.ts,specs.ts} src/modules/dev/KnowledgebasePage.tsx src/i18n/{format.ts,core.ts,I18nProvider.tsx} src/data/{tax.ts,payrollCalc.ts,expenseCalc.ts,MockProvider.ts,DataContext.tsx,seed/*} src/tenant/{tenant.ts,pricing.ts,brand.ts} src/design/{tokens.ts,tokens.css,library.ts} src/auth/roles.ts src/dev/inspectorBus.ts src/styles/global.css src/components/** (TopBar NavBar LangToggle ClassRow RosterRow Receipt PageStub DesktopShell LiveBlock Card Chip StatTile RoomDayGrid EmptyState Notice NotificationBell EmailPreview Toggle CapacityMeter Accordion GlobalSearch ListRow PhoneBubble RatingScale Drawer BarList ChapterCard MarkdownViewer OrderSummary Figure CountdownRing Wordmark.meta NavBar.meta) src/modules/admin/** src/modules/customer/** src/modules/staff/** src/modules/teacher/** src/modules/website/** src/modules/hub/** docs/flow-map.md docs/prompts/{0009-salvage-empty10.md,0013-ops-manual-visual-live.md,0020-polish-pass.md} docs/changelog/{0001-initial-build.md,0004-docs-content-canvas.md,0009-salvage-empty10.md,0020-polish-pass.md} docs/{README.md,architecture.md,kanban.md,rules/documentation.md} docs/pages/{M-08.md,K-03.md,C-07b.md} docs/ops-manual/{README.md,es/00-index.md,en/00-index.md} docs/screenshots/** README.md ROADMAP.md
codes: all surfaces (HUB-01 W-01…W-09 P-01 A-01…A-06 C-01…C-26 E-01…E-05 S-01…S-05 M-01…M-11 M-02a…d M-08a…f M-09a…c D-01…D-04 K-01…K-04)

## Verification (before → after)

| Measure | Before (a415fee) | After |
| --- | --- | --- |
| Main JS chunk | 2 549 kB (gzip 788 kB), 1 chunk | **796 kB** (gzip 259 kB), 235 chunks |
| `dist/` | 66 MB | 65 MB (screenshots still shipped, §F 31) |
| Unused exports (`audit-exports.mjs`) | 13 (+ 3 unused barrels) | **0** |
| Unused string keys (`audit-strings.mjs`) | 40 of 2 236 | **0** of 2 540 (0 missing `en`, 0 undefined) |
| Hardcoded literals in shared components (`audit-jsx.mjs`) | ~75 (60 in LiveBlock) | **0** (remaining hits are code fragments / dev tooling) |
| Raw token values in CSS (hex · rgba · px font-size · px radius · inline styles) | 4 · 6 · 22 · 8 · 5 | **0 · 0 · 0 · 0 · 0** |
| `toISOString().slice(0, 10)` / `T12:00:00` sites | 14 / 11 | **0 / 0** (`npm run test:dates` 10 / 10) |
| Horizontal overflow at 390 (`audit.mjs`) | 1 route (S-04) | **0** |
| Console errors / page errors, full screenshot pass (96 routes, 424 loads) | 0 | **0** (`npm run screenshots` exit 0; `--smoke` exit 0) |
| `npm run build` | clean | clean with `noImplicitReturns` + `noImplicitOverride` |
| `npm run sql` drift | none | none (48 tables) |
| Links offered to a role that cannot follow them | 4 (finance ×2, maintenance, visitor ×6 links) | **0** |

Before / after pairs (the "before" JPGs are the a415fee captures kept in the 0020 scratch folder; the "after" ones are the committed captures): M-09 `en-1280` (tiles `COP 15,326,0…` → `$15,326,000`, `approved` → Approved, `—` → Specials (no plan)), M-09 `es-390` (eyebrow no longer one letter per line, range chips one height), W-06 `en-1280` (footer `(pendiente) (pending)` → `El Poblado (pending)`), M-09c `es-1280` (status cell on one line, even rows), C-06 `es-1280` (one plan fills the row), C-02 `es-390` (filter strip fades instead of clipping), S-04 `es-390` (no horizontal scroll), C-04 `es-390` (a real class instead of "Clase no encontrada").
