version: 0.5.1
date: 2026-09-17
prompt: docs/prompts/0014-jas-feedback-0917.md
intent: Justin, relaying Jas's design review with annotated screenshots: "here are some feedback from Jas … Please apply her suggestions" and "i think she had some questions that I need to ask sergio. Please tell me what those are in english." Apply the four customer/staff points this worker owns (plan start and end date on the C-06 "Tu plan" card, a visible "Sin cupos" / "Full" state on a full class row, a "Valor pagado" + required "Observación" pair at the front desk, and the IVA notice out of the C-05 subtitle), hand the two website points to the workstream that holds `src/modules/website/**`, and write the reviewer's four questions down as owner decisions.
decision: Real data before new columns. The card Jas annotated ("Tu plan: Plan Mensual · Activa" + "Gestionar membresía") is the `Notice` at the top of C-06 `PlansPage`, not the C-22 page whose heading also reads "Membresía"; `memberships` already carries `starts_at` / `renews_at` / `ends_at` and the seed fills them, so point 1 is presentation only — "Inicio del plan" / "Fin del plan" (EN "Plan start" / "Plan end") through the app's `formatDate()`, with the year included because an annual cycle ends in another one, and "fin" resolving to `ends_at` when a cancellation is scheduled and to `renews_at` (the end of the paid cycle) otherwise. For the full-class state the capacity meter gives way to the pill rather than repeating itself: `ClassRow` derives `full` (`booked >= capacity` while `status === 'scheduled'`) and renders a danger `Badge` — the same family as the existing "Reservada" pill, existing tokens only — in the slot the "4 cupos" meter occupied, since a bar at 100 % adds nothing once the number is zero; `core.common.full` ES moves from "Lleno" to "Sin cupos" so the row, the week grid and check-in all use the reviewer's wording, and because booking must be unavailable the row now routes to the existing waitlist (C-20) on C-02 and C-01 instead of the booking action. `ClassRow.meta.ts` already listed `full` among its states — it is now real and its usage renders it. The seed guarantees one upcoming class today is full, applied deterministically **after** the random booking pass (and preferring people who already have an earlier class today) so no other page's demo data shifts. At the desk, "Valor pagado" defaults to the invoiced total and follows it while untouched (`paidRaw === null` means untouched), so the default sale is still one click; any other value reveals a required "Observación" and blocks "Completar venta" until it has text. Both are persisted: `payments.amount_paid` (nullable money) and `payments.note` are added to `src/data/schema.ts`, and the two generated artefacts were hand-edited to exactly the bytes `npm run sql` produces (verified by regenerating and diffing) so the concurrent session's new-table work merges cleanly. The C-05 subtitle drops the IVA fragment and keeps city and currency; nothing in the receipt or POS maths changed — S-04 still splits IVA from `settings.tax` and invoices still store subtotal/tax/total.
rejected: (1) Adding `starts_at` / `ends_at` columns as the brief allowed — `memberships` already had them plus `renews_at`; a second pair would have been two sources for one date. (2) Putting the dates on C-22 instead — its card is "Plan actual" and already shows "Socio desde" and "Renueva {date}"; the annotated card is C-06's. (3) Keeping the capacity meter *and* adding the pill — the row would have read "Sin cupos" twice. (4) Leaving `core.common.full` as "Lleno" and adding a second string for the pill — two words for one state, and the week grid would have disagreed with the row. (5) Leaving the full row pointing at the class detail (which already hides the booking button) — Jas asked for the action to be unavailable, and the app already has the waitlist to offer instead. (6) Forcing the full class in the seed by changing `n = r.int(4, mats)` inside the booking loop — it would have re-rolled the shared RNG stream and silently changed every other seeded page. (7) Formatting "Valor pagado" as grouped COP while typing — the field is a number entry; formatting it fought the caret. (8) A new `Textarea` atom for the observation — a one-line `Input` is enough and a new atom would need its own meta entry for no gain. (9) Running `npm run sql` and committing the regenerated files — the output is identical to the hand edit and a full regeneration risked reordering the file against the concurrent session's additions. (10) Touching `src/modules/website/**` for points 5 and 6 — the website workstream holds those files; doing them here would have produced a guaranteed conflict.
files: src/modules/customer/pages/PlansPage.tsx src/modules/customer/pages/PaymentMethodsPage.tsx src/modules/customer/pages/SchedulePage.tsx src/modules/customer/HomePage.tsx src/modules/customer/strings.ts src/modules/customer/customer.css src/modules/customer/payments.ts src/components/molecule/ClassRow/{ClassRow.tsx,ClassRow.css,ClassRow.meta.ts} src/i18n/core.ts src/modules/staff/RegisterPage.tsx src/modules/staff/strings.ts src/modules/staff/staff.css src/data/schema.ts src/data/seed/index.ts supabase/schema.sql docs/data-model.md docs/screenshots/{C-02,C-02b,C-05,S-01,S-02,S-04}/** ROADMAP.md docs/kanban.md docs/prompts/0014-jas-feedback-0917.md docs/changelog/0014-jas-feedback-0917.md
codes: C-06 C-02 C-02b C-01 C-05 S-04 (S-01 S-02 re-photographed — shared ClassRow) · W-01 and P-01 handed to the website workstream

## New and changed contracts
- `payments.amount_paid` (int, null) — what was actually received; equals `amount` unless `payments.note`
  explains why. `payments.note` (text, null) is **required at the desk** whenever the two differ (S-04 enforces it).
- `ClassRow` needs no new prop for the full state: it derives `booked >= capacity` on a scheduled session,
  renders the "Sin cupos" / "Full" pill in place of `CapacityMeter`, and adds `.classrow.is-full` for callers.
- `core.common.full` ES is **"Sin cupos"** (was "Lleno"); EN stays "Full".
- Callers that treat a row as a booking action must send a full class to the waitlist (C-20), as C-01 and C-02 do.

## Verification
- `npm run build`: zero TypeScript errors, `vite build` clean.
- `npm run screenshots -- --only=/app,/staff`: 33 routes, no console errors; C-02, C-02b, C-05, S-01, S-02, S-04
  changed (C-01/C-06/C-22 are captured as `usr_super`, who holds no membership, so those two cards do not appear
  in the capture — they are verified by the Playwright check below).
- Playwright check (ES, as `usr_cust` / `usr_desk`): C-06 notice reads "Inicio del plan: 27 de jul de 2026 ·
  Fin del plan: 27 de ago de 2026"; C-02 shows exactly one "Sin cupos" pill and clicking it lands on
  `/app/waitlist/ses_2026-09-17_3`; S-04 defaults "Valor pagado" to the total with the note field hidden and the
  CTA enabled, then on a changed amount reveals "Observación", blocks the CTA, and re-enables it once the note
  has text; C-05's header contains no "IVA".
- `supabase/schema.sql` and `docs/data-model.md` regenerated with `npm run sql` and diffed against the hand
  edit: byte-identical.
