version: 0.2.0
date: 2026-09-17
prompt: docs/prompts/0004-docs-content-canvas.md
intent: Land the documentation and content track — operations manual viewer with real chapters, corrected canvas v1.5 with regenerated specs, a purpose-built docs viewer, screenshot rules and pipeline, roadmap and README — in parallel with the customer and staff/admin passes.
decision: Ops manual chapters copied to docs/ops-manual/{es,en} and rendered by a new bilingual viewer (K-03) that follows the app language, extracts DECISIÓN PENDIENTE callouts into /manual/decisions (K-04) and renders [screenshot: …] lines as dashed placeholders (both features added to the shared MarkdownViewer, so every doc benefits). Canvas replaced by the audited v1.5 copy; specs.json/strings.json regenerated by a new evaluating extractor (scripts/extract-canvas.mjs) instead of the old regex pass; gen-specs keeps compatibility aliases for the retired C-07b and the shared 'C-14 / C-15' key so untouched stubs keep building. Docs viewer (K-02) got a grouped sidebar, kanban lanes×columns, changelog newest-first, prompt|response side by side and a screenshot gallery; the knowledgebase page reuses the same renderers. Screenshot rules rewritten (ES+EN, 390/1280, dark for key pages, before/after pairs, referenced from page doc and changelog, regenerate every session) with the new file naming docs/screenshots/<code>/<lang>-<width>[-dark].png, --only and --label flags, and a docs/pages template + generator. ROADMAP.md written with dependency-ordered phases P1–P7, definitions of done and 20 deduplicated open decisions; README updated.
rejected: Rewriting the manual markdown to HTML-specific syntax (kept plain markdown conventions so GitHub renders it too). A separate "callout" component per module (put callout and placeholder support in MarkdownViewer once). Deleting the C-07b / 'C-14 / C-15' keys outright (would break the customer stubs another worker owns mid-flight; aliases are removed when those stubs are replaced). A hand-maintained decisions list (extracted from the chapters at build time so it cannot drift). Running the full screenshot pass now (coordinator triggers it after the parallel passes merge, otherwise every capture would be redone).
files: docs/ops-manual/{README.md,es/*.md,en/*.md} docs/flow-map.md docs/rules/documentation.md docs/pages/{README.md,_TEMPLATE.md} docs/kanban.md docs/changelog/0004-* docs/prompts/0004-* reference/canvas/{Hoy Wellness System.dc.html,CANVAS-AUDIT.md,specs.json,strings.json} reference/README.md scripts/{extract-canvas.mjs,gen-specs.mjs,gen-page-doc.mjs,screenshots.mjs} src/specs/canvasSpecs.ts (generated) src/components/organism/MarkdownViewer/* src/modules/ops-manual/* src/modules/docs/* src/modules/dev/KnowledgebasePage.tsx ROADMAP.md README.md

## Codes touched
K-02 (docs viewer), K-03 (operations manual), K-04 (decisions pending, new), K-01 (knowledgebase renderers), C-02b / C-14 / C-15 (new separate specs), C-07b (retired → alias), D-02 (MarkdownViewer states: callouts, placeholder).

## Shared files touched (backwards compatible)
- `src/components/organism/MarkdownViewer/*` — new optional `components` prop; built-in callouts and placeholders. Existing callers unchanged.
- `src/specs/canvasSpecs.ts` — regenerated; `canvasCodes` now excludes compatibility aliases.
- `src/modules/dev/KnowledgebasePage.tsx` — reuses the docs renderers (allowed by the brief).

## Screenshots
Not captured in this pass on purpose (see rejected). After the parallel passes merge:
`npm run screenshots`, then `docs/pages/K-02.md`, `K-03.md`, `K-04.md` via `node scripts/gen-page-doc.mjs`.

[screenshot: K-03 — manual chapter 03 with sidebar, meta row, callout and placeholder box]
[screenshot: K-04 — decisions pending grouped by chapter]
[screenshot: K-02 — kanban lanes × columns]
