version: 0.5.0
date: 2026-09-17
prompt: docs/prompts/0009-salvage-empty10.md
intent: A second repository, `imagine-os/empty10`, turned out to hold a parallel vanilla-JS "HOY OS" build pushed by another Claude session on 2026-09-17 (commits 2c27395, cf7afaf, 407fbe0). Justin asked for it to be cleared unless something in it is important. Decide what — if anything — is worth keeping before the repo is reset or deleted.
decision: Keep only the migration and the plans as reference. Four files are copied into `reference/alt-build-empty10/` (frozen reference material, outside `tsconfig.json`'s `include`, never imported): `supabase/migrations/0001_init.sql` (53 tables, RLS on all 53, 188 per-table policies, 6 functions, 33 triggers, 109 indexes), `tools/gen-supabase.mjs` (the generator that emitted it from its schema + roles modules), `ARCHITECTURE.md` and `PLAN.md`. The migration is the input for the P2 Supabase phase: our `supabase/schema.sql` has 29 tables and a thinner policy set, so that file is the reference for RLS policy shape, naming and per-role coverage, and the generator is the reference for producing policies mechanically rather than by hand. `reference/alt-build-empty10/README.md` records the provenance, why each file is kept and that none of it is code the app uses. Its canvas and ops manual were deliberately not copied — ours are the ones the app renders. Nothing over 2 MB, no `node_modules`, no assets. All four files were scanned for keys, tokens, passwords and connection strings: the only hits are schema column names (`password_hash`, `push_token`, `integrations.secret_ref`) and comments stating those columns are never selected in the client, so nothing was withheld. The empty10 repo itself is left untouched, pending Justin's call (reset to placeholder, or delete), tracked under a new "Repo hygiene" heading in `docs/kanban.md`.
rejected: (1) Merge the two codebases — the other build is vanilla ES modules with no build step, its own token file, its own schema module and its own generator tooling; it shares no code with this repo, so a merge means reconciling two complete architectures (React + TypeScript strict + Vite vs browser ES modules) for no product gain, and would throw away the strict-TypeScript guarantee `npm run build` enforces. (2) Delete the repo outright as Justin's phrasing allowed — the RLS matrix is genuinely ahead of ours and would have been rewritten from scratch in P2. (3) Copy the whole repo into `reference/` — its canvas and ops manual duplicate ours and would create two sources of truth for the same material, and its `src/` and assets are dead weight. (4) Port the migration straight into `supabase/schema.sql` now — that file is generated from `src/data/schema.ts`, and P2 has not started; adopting another build's table shapes before our own schema is frozen would invert the extension point. Kept as reference to compare against instead.
files: reference/alt-build-empty10/README.md reference/alt-build-empty10/ARCHITECTURE.md reference/alt-build-empty10/PLAN.md reference/alt-build-empty10/supabase/migrations/0001_init.sql reference/alt-build-empty10/tools/gen-supabase.mjs docs/changelog/0009-salvage-empty10.md docs/kanban.md ROADMAP.md
codes: (none — docs and frozen reference material only; no routes, specs, components or strings touched)

## Verification
- `npm run build` passes with zero TypeScript errors (`tsconfig.json` includes only `src`, so nothing under `reference/` is compiled).
- No `src/` file changed, so no screenshot regeneration is needed.
