version: 0.6.2
date: 2026-09-17
prompt: docs/prompts/0017-especiales.md
intent: Justin: "we need a system for custom payments … private events can be booked and priced manually at front desk … seperate teacher payouts can also be made manually … schedule/mark the yoga studio or meditation room accordingly on the calendar for items besides just classes … on the website … say to please contact us direct for private events or special requests … so that edge cases have a method of being handled manually." Build the manual path: a hand-priced charge at the desk, a manual teacher payout that reaches payroll, a room calendar for things that are not classes, and website copy in the brand voice.
decision: **The system is called Especiales** (EN "Specials") — the word the desk already uses for "the thing that is not on the price list", identical in both languages, and it lives **inside the `espacio` family** whose "desde" items already end in a conversation (manual chapter 12) instead of inventing a parallel concept. (1) **Two additive tables.** `special_charges` (concept, amount, customer or a plain `contact_name`, optional `teacher_id` + `teacher_payout`, optional `space_booking_id`, the `payment_id` that carries the money in, `source_item` = the pricing.ts "desde" item it started from, note, created_by) and `space_bookings` (room, kind `private_event` · `rental` · `private_class` · `maintenance` · `blocked`, title, window, customer or contact, teacher, `special_charge_id`, status `held` → `confirmed` → `done` or `cancelled`, note, created_by), both with bilingual labels, `TableGroup` and a `TableDef.rls` contract (front desk / coordinator / admin / finance / super admin write; a teacher reads their own; a customer reads their own). `payroll_lines.kind` gains `manual` with a `special_charge_id` source, and `payments.user_id` becomes nullable so a company or a non-member can pay an Especial without a fake user row. (2) **S-04 sells it as one more item.** "Qué compra" gained the family **Espacio · Especiales**: the five "desde" `PriceRow`s (Sesión Privada, Talleres, Foto & Video, Rodajes, Pop-ups) and a dashed **Especial · concepto y precio manual** row. Picking one turns the sale into an Especial — a synthetic `PriceItem` whose name and price were typed by hand, so IVA, the summary rail, "Valor pagado", "Observación" (0014) and the receipt work untouched. The Especial card holds concept (prefilled from the "desde" name), agreed amount (prefilled from its price), teacher + payout (the per-class rate shown as reference), room + kind + date + window with a live conflict check, and an internal note; "Quién" gained a **Solo contacto** chip, enabled only for an Especial. Completing writes `payments` (`plan_id` null) + `invoices`, the `space_bookings` row (confirmed) when a room was set, the `special_charges` row joining the three, and an `audit_log` row per write. `?booking=<id>` preloads a booking S-05 hands over and confirms + links it on completion. (3) **One arithmetic, extended.** `src/data/payrollCalc.ts` gained `manualLinesFor(period, specials, spaceBookings, teachers)` — one `manual` line per Especial with a teacher and a payout whose *service date* (its room window, else the sale day) falls in the period, dropped when the booking is cancelled — and `draftLinesFor()` = class lines + manual lines. M-09a's `useGenerateDraft`, the seed's draft month and S-03 call it; the idempotency is unchanged (delete + recompute) and a manual line carries `special_charge_id` so it can never be added twice. The label **"Especial: <concept>"** is written into `note` by `manualLineNote()`, so M-09b, S-03 and the CSV agree without a join; M-09b prints the service date, a "3 especiales" count in the header and a one-line explanation. (4) **S-05 `/staff/rooms`** (new code, DesktopShell, roles front_desk / coordinator / admin / super_admin, `useLayout(S05)` with sections DateStrip · DayGrid · BookingForm · Selected · UpcomingList): a new **`RoomDayGrid` organism** (meta with two usages and eight states) draws rooms from the `rooms` table as columns and hours as rows, classes tinted by movement and bookings by kind (highlight for events, sky for rentals, deep for private classes, linen for maintenance and blocks; held = dashed, cancelled = struck, done = dimmed) with a "now" line; a 14-day `DateStrip` plus any date; a form whose **conflict check** (`findConflicts()` in `src/modules/staff/rooms.ts`, shared with S-04) lists the class or booking in the way and disables both CTAs; confirm / done / cancel actions, each an `audit_log` row; and "Cobrar (S-04)". Linked from S-01 quick actions and the staff nav ("Salas"). The seed gains a second room, **Sala de meditación** (8 mats, demo), and four bookings + two Especiales so every screen opens with data — one delivered birthday session whose payout is the manual line of the current draft. (5) **S-03** home lists the teacher's own space bookings with the agreed payout (or "pago por definir"); the payroll statement labels manual lines "Especial: <concept>" with a highlight badge. (6) **Website copy** in `brand.ts` voice: P-01 gets a `Specials` section ("Especiales · Lo que no cabe en un plan"), W-06 a `Specials` card, C-06 one line under "¿Prefieres pagar por clase?", all opening WhatsApp from `tenant.contact` with a prefilled message, nothing sold on the site. (7) **Manual**: chapter 12 §3 now books in S-05 with the conflict rule and a new §7 "Especiales" (how to book, charge, the payout, the statuses), chapter 10 points at the Especial item, chapter 16 explains manual lines — ES and EN, version 0.6.2. (8) **Generator fix**: `scripts/gen-sql.mjs` emits a foreign key to a table created later in the file (and the `special_charges` ⇄ `space_bookings` pair) as `alter table … add constraint` after all tables, so `supabase/schema.sql` applies in order. Jas's item 34 for Lore is answered structurally — a birthday or event group session is an Especial priced by hand; the commercial product is still hers to decide.
rejected: (1) A new plan family or a `pricing.ts` item called "Especial" — a price that is typed at the desk is not a price the model owns; the Especial starts *from* the existing "desde" items instead. (2) A separate "Especiales" page for charging — S-04 already has who / what / how, IVA, "Valor pagado" and the receipt; a second register would have drifted. (3) Storing the manual payout directly as a `payroll_lines` row at sale time — the run may not exist yet and finance regenerates drafts; deriving the line from `special_charges.teacher_payout` keeps `payrollCalc.ts` the single arithmetic and regeneration idempotent. (4) Reusing `class_sessions` with a special modality for room blocks — a rental has no capacity, no bookings and no attendance, and it would have polluted S-02, the customer schedule and the payroll class count. (5) Reusing `events` (C-23) — an event is published and sold by RSVP; an Especial is private and priced by hand. (6) A required user row for every payer — a company renting the room is not a member; `payments.user_id` nullable + `contact_name` is the honest shape, and the one place that assumed a member (M-09's member column) now says "contacto (Especial)". (7) A week view for S-05 — the rooms × hours day grid is what answers "is the sala free at 5 on Saturday"; the strip plus any-date input covers navigation. (8) Blocking the S-05 booking on the deposit — the `held` status draws the quoted-but-unpaid window dashed instead, matching chapter 12 ("sin anticipo la fecha no se bloquea") without hiding the conversation. (9) Adding a `DECISIÓN PENDIENTE` for the real room list — §E already carries 34 decisions and the seed room is visibly demo; noted here and in ROADMAP §F instead. (10) An exclamation mark anywhere in the website copy.
files: src/data/schema.ts src/data/payrollCalc.ts src/data/seed/{index.ts,catalog.ts,payroll.ts,specials.ts} scripts/gen-sql.mjs supabase/schema.sql docs/data-model.md docs/roles.md src/components/organism/RoomDayGrid/{RoomDayGrid.tsx,RoomDayGrid.css,RoomDayGrid.meta.ts} src/modules/staff/{RoomsPage.tsx,RegisterPage.tsx,HomePage.tsx,rooms.ts,specs.ts,index.ts,strings.ts,staff.css} src/modules/admin/{payouts.ts,PayoutsPage.tsx,FinancePage.tsx,specs.ts,strings.ts,admin.css} src/modules/teacher/{HomePage.tsx,PayrollPage.tsx,specs.ts,strings.ts} src/modules/website/{specs.ts,strings.ts,pages/PlansPage.tsx,pages/ContactPage.tsx} src/modules/customer/{specs.ts,strings.ts,pages/PlansPage.tsx} src/i18n/core.ts docs/ops-manual/{es,en}/{10-ventas-y-planes.md,12-espacio-b2b.md,16-nomina-y-payouts.md} docs/pages/S-05.md docs/screenshots/{S-05,S-04,S-01,S-03,M-09a,M-09b,P-01,W-06,C-06}/** docs/screenshots/routes.json docs/prompts/0017-especiales.md docs/changelog/0017-especiales.md docs/kanban.md ROADMAP.md README.md package.json
codes: S-05 (new) S-04 S-01 S-03 M-09a M-09b M-09 P-01 W-06 C-06

## New and changed contracts
- **Tables (2 new, 2 changed).** `special_charges` and `space_bookings` (see `docs/data-model.md`); `payroll_lines.kind`
  gains `manual` + `special_charge_id`; `payments.user_id` is nullable (null only for an Especial paid by a
  non-member contact — `special_charges.contact_name` says who). 42 → **44 tables** on this branch, **46** on main with 0016.
- **`src/data/payrollCalc.ts`**: `manualLinesFor`, `draftLinesFor`, `specialServiceDate`, `manualLineNote`; `DraftLine`
  gains `special_charge_id`. Anything that computes a period's payroll calls `draftLinesFor`, not `classLinesFor` alone.
- **`src/modules/staff/rooms.ts`**: `findConflicts(window, sessions, bookings)` is the one definition of "the room is
  taken" (scheduled class or non-cancelled booking, overlap > 0 min, back-to-back allowed); `KIND_LABEL`, `STATUS_LABEL`,
  `KIND_FOR_ITEM` (which "desde" item maps to which booking kind), `localIso` / `dateInputValue` / `timeInputValue`.
- **`RoomDayGrid`** (organism, D-02): `rooms`, `blocks: RoomBlock[]` (tone = movement or event / rental / private /
  maintenance / blocked; status = class / held / confirmed / cancelled / done), `onSelect`, `onSlot`, `now`.
- **Routes**: `/staff/register?booking=<space_booking_id>` preloads an Especial from a booking; `/staff/rooms?date=YYYY-MM-DD&booking=<id>`.
- **Audit actions**: `special.create`, `space_booking.create`, `space_booking.confirmed` / `.done` / `.cancelled`.
- **Strings**: `core.nav.rooms`, `core.common.now`, `core.common.capacity`; `staff.register.especial.*`,
  `staff.register.contact*`, `staff.rooms.*`; `admin.payouts.kind.manual`, `admin.payouts.manualHint`,
  `admin.payouts.runOf.manual`, `admin.finance.col.contact`; `teacher.payroll.kind.manual`, `teacher.home.specials*`;
  `site.plans.specials.*`, `site.contact.specials*`; `customer.plans.specials*`.
- **`scripts/gen-sql.mjs`** defers a forward or circular FK to an `alter table … add constraint` block after all
  `create table`s (six constraints today: consents, legal_acceptances, space_bookings, bookings, event_rsvps, credits).

## Website copy (ES / EN)
- P-01 · eyebrow **Especiales** · title **Lo que no cabe en un plan** / **What does not fit in a plan** · "Un cumpleaños,
  una sesión para tu equipo, un evento privado o algo que todavía no tiene nombre. Esas cosas no se compran con un botón:
  se conversan. Escríbenos y lo armamos contigo." / "A birthday, a session for your team, a private event or something that
  does not have a name yet. Those things are not bought with a button: they are talked through. Write to us and we will
  shape it with you." · CTA **Escribirnos por WhatsApp** / **Write to us on WhatsApp**.
- W-06 · **Especiales** / **Specials** · "Eventos privados, cumpleaños, sesiones para equipos y pedidos especiales se
  arman directamente con el estudio. Cuéntanos qué tienes en mente y te respondemos por WhatsApp." / "Private events,
  birthdays, team sessions and special requests are arranged directly with the studio. Tell us what you have in mind and
  we will answer on WhatsApp."
- C-06 · "¿Un cumpleaños, un evento privado o una sesión para tu equipo? Eso lo armamos directamente contigo." /
  "A birthday, a private event or a session for your team? That we shape directly with you." · **Escríbenos** / **Write to us**.
- Prefilled WhatsApp text: "Hola HOY, quiero preguntar por un especial: un evento privado o una sesión de grupo." /
  "Hi HOY, I would like to ask about a special: a private event or a group session." The number is `tenant.contact.whatsapp`.

## Screenshots
- S-05: `docs/screenshots/S-05/{es,en}-{390,1280}.jpg` (new). S-04 with an Especial in progress:
  `docs/screenshots/S-04/es-1280-especial.jpg` (labelled capture, as `usr_desk`). M-09b with three manual lines:
  `docs/screenshots/M-09b/es-1280-especial.jpg` (labelled, as `usr_fin`, taken right after regenerating the draft).
- Re-photographed: S-01, S-04, S-03 (home + payroll), M-09a, M-09b, P-01, W-06, C-06 — `npm run screenshots -- --only=…`
  over 10 routes, no console errors; `docs/screenshots/routes.json` lists 91 routes after the rebase onto 0016 (S-05 and M-09c both new).

## Verification
- `npm run build`: zero TypeScript errors (strict), `vite build` clean (the pre-existing single-chunk size warning only).
- `npm run sql`: 44 tables written to `supabase/schema.sql` and `docs/data-model.md` on the branch; **46** after the rebase onto 0016 (`expense_templates`, `expenses`), regenerated and rebuilt before the push.
- `npm run screenshots -- --smoke`: exit 0, no console errors (run before the commit; see the commit body for the count).
- Playwright check (`playwright-core` against `vite preview` on port 4184, ES, mock DB carried between contexts),
  **20 / 20 pass, no console or page errors**:
  - as `usr_desk`, S-04: picking "Sesión Privada", "Solo contacto", a teacher (Andrés) with payout 200.000, Sala principal
    tomorrow 20:15–21:15 shows "libre", and "Completar venta" writes exactly one `payments` row (`plan_id` null,
    `user_id` null, approved, 600.000), one `invoices` row, one `special_charges` row (payout 200.000, `source_item`
    `privada`, linked to payment and booking) and one confirmed `space_bookings` row (`private_class`, linked back), with
    `payment.take`, `space_booking.create` and `special.create` in `audit_log`;
  - S-05: a 20:30–22:00 window in the same room lists the new booking as a conflict and disables "Reservar confirmada";
    moving the start to 21:30 enables it and "Dejar en espera" writes a `held` row;
  - as `usr_fin`, M-09a "Generar borrador" for September lands on M-09b with **3 manual lines** (the seeded birthday
    150.000 and team session 180.000 plus the new 200.000 for Andrés, each with `special_charge_id`); **run total
    3.015.000 = classes 2.485.000 + manual 530.000**; the page prints three "Especial: …" lines; "Recalcular borrador"
    leaves 29 lines / 3 manual (idempotent) and touches no other run;
  - as `usr_teach` (Andrés), `/teach/payroll` shows "Especial: Sesión privada · equipo Lumen (verificación) · $ 200.000"
    and `/teach` lists the booking under Especiales with "pago $ 200.000";
  - `/staff/register?booking=spb_foto` preloads the held photo-shoot booking's title, and completing the sale flips it to
    `confirmed` with its `special_charge_id` set.
- Not verified here: dark-mode captures of S-05 (not a key page), the Supabase apply of the deferred FK block (no
  Postgres in this environment — reviewed by reading the emitted SQL).
