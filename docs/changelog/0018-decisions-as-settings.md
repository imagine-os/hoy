version: 0.7.0
date: 2026-09-17
prompt: docs/prompts/0018-decisions-as-settings.md
intent: Justin: "Decisions only you can make (all in ROADMAP §E): real address/WhatsApp/email; teacher rate card; payroll monthly vs biweekly; which legal versions to publish; map provider; IVA included in published prices or not; whether Respiración is its own class … Give me settings for these to be filled in … something like payroll monthly v biweekly is a feature switch you can program for both. … you can have the integrations page ready to fill in and finalize the connections later when our dev gets to it." Plus his top three site ideas for "the longer term plan … when there's nothing else to do".
decision: **Every §E decision that is a value became a field; every one that is a behaviour became a switch programmed both ways.** (1) **M-08a General — contact identity.** `settings.profile` gained city (default Medellín), Instagram handle + URL, map lat/lng, map label, a Google Maps link and a **`confirmed`** switch next to address, WhatsApp, email and NIT. `useContact()` / `contactOf()` in `src/modules/admin/settings.ts` is the one reader — M-08a first, `src/tenant/tenant.ts` as the default for every empty field — and `pending = !confirmed`. Every consumer that read `tenant.contact` / `tenant.location` / `tenant.social` directly now reads it: the site footer (`SiteShell`, with `useWaHref()`), W-06, `MapSlot` (coordinates and link), the legal `{{tenant.*}}` tokens, the M-04 email footer, C-25 / C-06 / C-16 / C-05 / A-02 contact rows, S-03's "ask finance" link and the manual's `{{tenant:contact}}` block, each printing the *pendiente / pending* label until the switch is on. `tenant.ts` stays the defaults file. (2) **M-08c Payments — the payroll card.** `pricesIncludeIva` is unchanged and now documented on the page as the driver of S-04 `splitTax()`, C-04 `OrderSummary` and the P-01 "¿Incluye IVA?" card. New `settings.payroll`: **`cadence: monthly | biweekly`** — `payrollCalc.periodsFor()` returns one period per month or two (1–15, 16–end), `periodAt()` steps by period, `isWholeMonth()` / `periodsOverlap()` label and guard; M-09a's generator produces one run per period of the chosen month (button reads "Generar borradores (2)" when biweekly) and **replaces a draft of the other cadence that overlaps the same days** (an approved or paid one blocks with its status), so no class can be paid twice; `periodLabel()` prints "septiembre 2026" or "1 – 15 sept 2026" on M-09a, M-09b and S-03; S-03 navigates by period and says "Total de la quincena"; M-09's default range is 30 d or 15 d until the user picks; `{{policy:payroll_cadence}}`, `{{policy:payout_method}}` and `{{policy:signed_by}}` render in the manual. **`rateCard: { byModality, byTeacher }`** — `payrollCalc.rateFor(teacher, modality, teachers, card)` resolves teacher override → modality rate → `teachers.rate_per_class` → 0, and `classLinesFor()` / `draftLinesFor()` take the card; M-09a, the seed (`buildPayroll`, including the reconstructed historic months) and S-03 all pass it, so the three still compute one number. The seed's 80–110k moved to `SEED_RATE_CARD` on the seeded `tenants.settings` row (by modality); the profile rates stay as the last fallback. `payoutMethod` (what M-09a proposes), `signedBy` (printed on M-09b) and `withholding` complete the card. (3) **M-08f Content (new code, `/admin/settings/content`, admins + coordinator).** `settings.content`: **`publicNaming: disciplines | movements`** (`classDisplay()` titles a C-03 / W-04 row by the modality or by the movement with the modality moved next to the teacher); **`breathworkOwnClass`** — the seed now has a **`respiracion` modality row** (45 min, libera) and `brand.ts` joins it (`modalitySlugs: ['respiracion']`); `visibleModalities()` / `useVisibleModalities()` hide the row on W-03, W-07, W-08 and the C-03 filter while the switch is off, so W-08 keeps its "lives inside the guided classes" sentence and prints facts when on; **`mapProvider: none | osm | google`** read by `MapSlot` when no prop is passed, with a live preview on the page; and a **legal versions** card — every `legal_documents` row with status, effective date, "requires acceptance" and a publish toggle that flips `status` + `published_at` (A-06 already shows the newest published version per kind), each flip an `audit_log` `legal.publish` / `legal.unpublish`. (4) **M-10 Integraciones (new code, `/admin/integrations`, super_admin / admin, nav order 21).** A new **`integrations` table** (`key` enum wompi · whatsapp · email · dian · maps · supabase, `status` simulated · configured · connected, `config` json of NON-SECRET fields, `notes`, `updated_by`; RLS: admin full, finance read) seeded with six simulated rows. `src/modules/admin/integrationDefs.ts` (pure, imported by the seed) defines per integration the fields (merchant id, public key, settlement / dispersion account last-4; sender number, WABA id, phone number id, template namespace, approved name; email provider, domain, From; DIAN provider, issuer NIT, resolution, prefix; place id, share link; project URL, anon key, region), the "what is simulated today" sentence, the dev checklist, the **secrets named but never stored** (`WOMPI_PRIVATE_KEY`, `META_WA_TOKEN`, `EMAIL_API_KEY`, `DIAN_PROVIDER_TOKEN`, `SUPABASE_SERVICE_ROLE_KEY`…), the screens that read it and the manual slug. The page (`useLayout(M10)`: Intro · Cards · Order) shows a status count per chip, the "keys live server-side" notice, one card per system with fields, "n of m filled", checklist, notes, status select and an audited save (`integration.update`), and the connection order from ROADMAP §B. M-08a's old "Integraciones" section became a pointer card; M-09a's "simulado" badge reads the Wompi row (`useIntegrationStatus`). (5) **ROADMAP**: §A bumped to 0.7.0 with the 0018 bullet; §E items 2, 11, 21, 22, 27, 30 and 31 marked "→ setting in M-08x (fill in)"; §F 15 rewritten (the blockers that are now fields), §F 14 and 20 updated; new **§G — When nothing else is queued**: SEO/OG + prerender, the "next class in N minutes" widget, the membership calculator, then the rest of `docs/website-vision.md` as one-liners. (6) **Manual** ES + EN: chapter 26 rewritten around M-10 (how to read a card, the state table with `{{table:integrations}}`, what can be promised, the order, where each fillable setting lives, three figures), chapter 16 §2 and §6 (the cadence switch, the rate card, `{{policy:payroll_cadence}}` / `{{policy:payout_method}}`, the decision reworded as "fill in M-08c"), chapter 01's decision now points at M-08a and the confirmed switch. (7) **Strings**: `core.nav.integrations`; ~70 `admin.settings.*`, `admin.integrations.*`, `admin.payouts.*` keys; `teacher.payroll.total.period`.
rejected: (1) Editing `src/tenant/tenant.ts` by hand once the owner answers (the 0.6.0 plan) — the owner cannot edit a TypeScript file and the values would still ship as a deploy; a setting with a "confirmed" switch keeps `tenant.ts` as the honest default and makes the answer a form. (2) A `teacher_rates` table — the rate card is a small map the owner edits in one card, it changes with the modality list, and `tenants.settings` already carries every other M-08 value; a table would have added a join to `payrollCalc` for nothing. Precedence teacher → modality → profile keeps `teachers.rate_per_class` meaningful as the last fallback instead of deleting a column that has data. (3) Deriving the cadence from the existing "15 días" range — a filter is not a policy; the switch is the policy and the range follows it. (4) Generating the two biweekly runs as one run with a mid-month split line — M-09b, approvals, payments and the CSV are per run; two runs is what "paid twice a month" means. (5) Letting a monthly draft and two biweekly drafts coexist for the same month — that is the one way to pay a class twice; the generator replaces an overlapping *draft* and refuses when the overlap is approved or paid. (6) Storing secrets in `integrations.config` "for the dev's convenience" — the M-08c notice has said since 0.5.0 that keys live server-side; the table has no column for them and the card names the environment variables instead. (7) A per-integration sub-page — six cards on one page is the checklist the owner asked for; the manual chapter is the long form. (8) A `DECISIÓN PENDIENTE` for each new setting — the decisions already exist in §E; each is marked "→ setting (fill in)" instead of flagged twice. (9) Parenthetical section names in the M-10 layout (`'Intro (title · …)'`) — `useLayout()` matches section names exactly, so the first pass rendered no cards; plain names, description in `notes`. (10) Adding Respiración to the weekly timetable — the owner has not said it is taught as a class; the row exists so the switch has something to show, the schedule is unchanged.
files: src/data/schema.ts src/data/payrollCalc.ts src/data/seed/{index.ts,catalog.ts,payroll.ts,integrations.ts} src/tenant/brand.ts supabase/schema.sql docs/data-model.md src/modules/admin/{settings.ts,SettingsPage.tsx,IntegrationsPage.tsx,integrations.ts,integrationDefs.ts,payouts.ts,PayoutsPage.tsx,FinancePage.tsx,EmailsPage.tsx,specs.ts,index.ts,strings.ts,admin.css} src/modules/teacher/{PayrollPage.tsx,HomePage.tsx,strings.ts} src/modules/website/{SiteShell.tsx,pages/ContactPage.tsx,pages/PlansPage.tsx,pages/HomePage.tsx,pages/SchedulePage.tsx,pages/ClassesPage.tsx,pages/ClassDetailPage.tsx,pages/ModalitiesPage.tsx} src/modules/customer/{legal.tsx,auth/LockedPage.tsx,pages/PlansPage.tsx,pages/ProfilePage.tsx,pages/FaqPage.tsx,pages/PaymentMethodsPage.tsx,pages/MorePage.tsx,pages/SchedulePage.tsx} src/components/molecule/MapSlot/{MapSlot.tsx,MapSlot.meta.ts} src/components/organism/LiveBlock/LiveBlock.tsx src/i18n/core.ts docs/ops-manual/{es,en}/{26-integraciones.md,16-nomina-y-payouts.md,01-quienes-somos-y-filosofia.md} docs/pages/{M-08f.md,M-10.md,M-08a.md,M-08c.md} docs/screenshots/{M-08a,M-08b,M-08c,M-08d,M-08e,M-08f,M-10,M-09a,M-09b,P-01}/** docs/screenshots/routes.json docs/website-vision.md docs/prompts/0018-decisions-as-settings.md docs/changelog/0018-decisions-as-settings.md docs/kanban.md ROADMAP.md README.md package.json
codes: M-08f (new) M-10 (new) M-08a M-08c M-09a M-09b M-09 S-03 S-01 W-01 W-04 W-06 W-08 W-03 W-07 P-01 C-03 C-25 C-06 A-06 K-03

## New and changed contracts
- **Table (1 new).** `integrations` (see `docs/data-model.md`). 46 → **47 tables** on this branch; **48** on main after the rebase onto 0019 (`deletion_requests`).
- **`StudioSettings`** (`src/modules/admin/settings.ts`): `profile` gains `city, instagram, instagramUrl, mapLat, mapLng, mapLabel, mapLink, confirmed`; new sections **`payroll`** (`cadence, payoutMethod, signedBy, withholding, rateCard`) and **`content`** (`publicNaming, breathworkOwnClass, mapProvider`); `usePolicy()` exposes `payroll`. New readers: `useContact()` / `contactOf()`, `waLinkFor()`, `visibleModalities()` / `useVisibleModalities()`, `classDisplay()`, `BREATHWORK_SLUG`.
- **`src/data/payrollCalc.ts`**: `PayrollCadence`, `RateCard`, `EMPTY_RATE_CARD`, `rateFor()`, `periodsFor()`, `periodContaining()`, `periodAt()`, `isWholeMonth()`, `periodsOverlap()`; `classLinesFor(…, card)`; `DraftInputs.rateCard`. Anything that computes a rate calls `rateFor`, anything that names a pay period calls `periodsFor` / `periodAt`.
- **`src/modules/admin/payouts.ts`**: `periodsForMonth(cadence, offset)`, `periodLabel(period, lang)`; `useGenerateDraft()` reads the rate card, returns `replacedOverlap`, replaces overlapping drafts and blocks on an overlapping approved/paid run.
- **`src/modules/admin/integrationDefs.ts`** (pure) + **`integrations.ts`** (hooks: `useIntegrations()`, `useIntegrationStatus(key)`).
- **`MapSlot`**: `provider` optional → M-08f setting; location and link from `useContact()`.
- **`SiteShell`**: `useWaHref()` (hook) beside `waHref()`; the footer WhatsApp link carries `data-testid="footer-whatsapp"`.
- **Routes**: `/admin/settings/content` (M-08f), `/admin/integrations` (M-10, nav "Integraciones").
- **Audit actions**: `integration.update`, `legal.publish`, `legal.unpublish`; `settings.update` now also for `payroll` and `content`.
- **Seed**: `mod_respiracion`; `SEED_RATE_CARD`; `tenants.settings.payroll` on the tenant row; six `integrations` rows.

## Screenshots
- New: `docs/screenshots/M-08f/{es,en}-{390,1280}.jpg`, `docs/screenshots/M-10/{es,en}-{390,1280}.jpg`.
- Re-photographed (`npm run screenshots -- --only=/admin/settings,/admin/integrations,/admin/finance/payouts,/site/plans`, 10 routes, no console errors): M-08a (contact fields + confirmed switch + M-10 pointer), M-08b, M-08c (payroll card with cadence and rate card), M-08d, M-08e, M-09a (cadence line under the generator), M-09b, P-01.
- Not re-photographed (footer text unchanged in the default state; the pending label was already there): the other site pages, S-03, C-03.

## Verification
- `npm run build`: zero TypeScript errors (strict), `vite build` clean (the pre-existing single-chunk size warning only).
- `npm run sql`: 47 tables written to `supabase/schema.sql` and `docs/data-model.md` on the branch; 48 after the rebase onto 0019, regenerated and rebuilt before the push.
- `npm run screenshots -- --smoke`: exit 0, no console errors.
- Playwright flow (`playwright-core` against `vite preview`, ES unless noted, one browser context so the mock DB carries across pages, full page loads between role switches), **27 / 27 pass, no console or page errors**:
  - setup: the seeded September draft removed so S-03 shows the live estimate; `integrations` seeded with 6 simulated rows; the `respiracion` modality row exists;
  - as `usr_teach`, S-03 shows the estimate; as `usr_super`, M-08c Hot Vinyasa rate 110.000 → **130.000** saved; S-03 re-rendered prints 130.000 and a different total (**a rate edit moves the estimate**);
  - as `usr_fin`, M-09a "Generar borrador" (monthly) → exactly one draft `2026-09-01…2026-09-30` = **3.125.000**, with Hot Vinyasa class lines at 130.000;
  - M-08c cadence → **quincenal**, saved; M-09a's button reads "Generar borradores (2)"; generating → **two drafts, `09-01…09-15` = 2.240.000 and `09-16…09-30` = 885.000, sum 3.125.000 = the monthly total**; the monthly draft was replaced; M-09a lists "1 – 15 sept"; S-03 shows the quincena; M-09 opens on "15 días";
  - M-08a WhatsApp → `+57 311 222 3344`, saved; `/site` footer prints it with "(pendiente)" and its href is `https://wa.me/573112223344`; W-06 shows it;
  - M-08f "Respiración es una clase propia" on → W-08 `/site/classes/respiracion` shows "45 min" (it did not before); the first legal publish toggle → 4 of 7 published + `legal.publish` in `audit_log`;
  - M-10 renders in ES (6 cards) and in EN; filling Wompi's merchant id and setting "Configurado" writes `config.merchantId`, `status` and `integration.update`.
- Not verified here: the OSM / Google embeds (the network is blocked in this environment; the `<iframe>` src is built as before), a Postgres apply of the new table (reviewed by reading the emitted SQL).
