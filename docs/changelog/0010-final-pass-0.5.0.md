version: 0.5.0
date: 2026-09-17
prompt: docs/prompts/0010-final-pass-0.5.0.md
intent: Justin: "continue building better and deeper … let me know if there's more work after this pass". Close v0.5.0: pick up the follow-ups the admin-shell (0007) and data-depth (0008) tracks left for the integrator, refresh every screenshot so the captures match the shipped screens, and write down — in one place, in plain language — what is still missing so the owner can decide what to fund next.
decision: One source for the policy numbers. `src/modules/customer/policy.ts` no longer keeps its own provider subscription: `attachPolicy()` (a bespoke peek + `get` + `subscribe('tenants')` loop) is deleted and the module now reads `usePolicy()` from `src/modules/admin/settings.ts`, the live reader 0007 exported, which follows `tenants` through `useTable()`. `<PolicySync/>` is kept, because `usePolicy()` does not replace it: it is a hook, and eighteen customer files plus the non-React helpers `cancelDeadline()` / `insideCancelWindow()` read `policy.*` as a plain value. `PolicySync` is now a two-line bridge (`current = usePolicyValues()`) mounted above the router, so the snapshot is refreshed during its render rather than in an effect — the routed pages below it render in the same pass and the first paint already shows stored values, which the old effect-based sync could not guarantee. The public API of the customer module is unchanged (`policy`, `currentPolicy()`, `policyFromSettings()`, `cancelDeadline()`, `insideCancelWindow()`, `HOUR`, `MINUTE` all still export the same shapes) and `usePolicyValues()` is added as the hook new components should prefer. `toPolicyValues(policies, tax)` is the single mapper both entry points share, so the M-08 → PolicyValues mapping exists once. On the version question the two 0.5.0 changelog entries raise: 0007 and 0008 were built in parallel and shipped in the same release, so **both stay at 0.5.0** and this entry is the third — there is no 0.5.1 between them, and ROADMAP §A now says so explicitly. Documentation: a full screenshot pass (80 routes, 69 codes, ES/EN × 390/1280 + dark for key pages) replaces the captures that still showed the pre-0008 screens: all 308 were retaken and 109 changed — the nine pages 0008 rewired (C-05, C-10, C-13, C-14/C-15, C-16, C-19, C-23, C-24, S-03) plus pages the 0007 containment pass shifted. M-08a…M-08e were already captured by 0007 and came back byte-identical, which is the check that the split is stable. `scripts/gen-page-doc.mjs --all` wrote the five missing sub-page docs, and `docs/pages/M-08.md` became the family index (the single-page captures were retired with the layout they documented, so its dead image links are replaced by links to the five sub-pages). ROADMAP §A is rewritten for 0.5.0, the Data lane moves to done with the six leftovers listed as numbered P1 items, and a new **§F "What remains after this pass (for Justin)"** is a plain numbered list in four groups: (a) six integrations still mocked behind their seams with the dependency order spelled out, (b) eight screens whose page doc or spec still says "placeholder", (c) the owner decisions with a pointer to `/#/manual/decisions` and the canvas audit, (d) repo hygiene including the pending empty10 call. README moves to 0.5.0.
rejected: (1) Converting the eighteen `import { policy }` call sites to `usePolicyValues()` — the literal reading of "switch policy.ts to usePolicy()", but `hooks.ts`, `cancelDeadline()` and `insideCancelWindow()` are called from callbacks and module scope where a hook cannot run, so it would have meant threading policy values through every helper signature: a large diff across the customer module for no behavioural gain, against the brief's "keep the customer module API stable". The snapshot bridge keeps one subscription and one mapper, which was the actual duplication. (2) Deleting `<PolicySync/>` as the brief allowed — it is what feeds the snapshot; removing it would freeze every customer page on `DEFAULT_SETTINGS` and silently ignore M-08. What `usePolicy()` replaced was `attachPolicy()`, and that is gone. (3) Keeping `policyFromSettings()` and adding a second parallel mapper for the hook's return — two mappings of the same eight fields drift; `toPolicyValues()` is called by both. (4) Renumbering 0008 to 0.5.1 to resolve the version collision — they shipped together; inventing a release boundary after the fact would misdate the tag and break the "one version per release" reading of the changelog. (5) Re-running only the routes 0007 and 0008 touched (`--only=`) — cheaper, but the containment pass changed shared card CSS on every surface, so every capture was potentially stale; the full pass took ~9 minutes and the result is 39 MB, well inside the ~70 MB budget. (6) Capturing the collapsed-sidebar and drawer states — `scripts/screenshots.mjs` has no state parameter (`--smoke`, `--only`, `--label`, `--quality` only), and adding one is a tooling change this pass did not need; the states are verified by hand and recorded in 0007. Left as a follow-up. (7) Deleting `docs/pages/M-08.md` outright — M-08 is still the parent canvas spec the five sub-pages extend, and the ops manual and specs refer to it; it is the family index now. (8) Writing §F as prose — Justin reads it to decide what to fund, so it is a numbered list where every item names the blocker and the phase.
files: src/modules/customer/policy.ts src/modules/admin/settings.ts ROADMAP.md README.md docs/pages/{M-08.md,M-08a.md,M-08b.md,M-08c.md,M-08d.md,M-08e.md} docs/screenshots/** (308 retaken, 109 changed, docs/screenshots/M-08/ retired) docs/kanban.md docs/prompts/0010-final-pass-0.5.0.md docs/changelog/0010-final-pass-0.5.0.md
codes: M-08 M-08a M-08b M-08c M-08d M-08e C-04 C-05 C-08 C-08b C-16 C-20 C-22 C-23 A-02 E-02 E-04 C-21 (+ every routed code re-photographed)

## New and changed contracts
- `src/modules/customer/policy.ts` exports `usePolicyValues(): PolicyValues` — the hook form, preferred in new
  components. `attachPolicy(data)` is **removed** (it was only ever called by `PolicySync`).
- `<PolicySync/>` must stay mounted inside `DataProviderRoot` and **above** the router; it assigns the snapshot
  during render so the pages below it see stored values on the first paint.
- `docs/pages/M-08.md` is a family index, not a page doc. New settings sub-pages get their own `M-08x.md`.

## Verification
- `npm run build`: zero TypeScript errors, `vite build` clean.
- `npm run screenshots`: 80 routes, no console errors, 308 `.jpg` captures retaken across 72 code folders, 109
  of them changed; `du -sh docs/screenshots` = 39 MB (budget ~70 MB).
- Policy rewiring checked against the M-08a save path: the cancellation-window, waitlist-claim, payment-hold,
  pause-cap, charge-notice and lockout numbers now arrive through the same `useTable('tenants')` subscription the
  admin uses, and the IVA fraction through `tax.ivaPct`.
- ROADMAP.md is 265 lines (budget ~300).
