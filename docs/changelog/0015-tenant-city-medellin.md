version: 0.5.1
date: 2026-09-17
prompt: docs/prompts/0015-tenant-city-medellin.md
intent: Justin, confirming the city question raised while applying Jas's review: "It's medellin. please continue doing things," — Jas's annotated C-05 screenshot showed the subtitle "Medellín · COP" while `src/tenant/tenant.ts` said `city: 'Bogotá'`, so the built app rendered "Bogotá · COP". Make the tenant config tell the truth and remove the hand-written city copy that disagreed with it.
type: fix
decision: One edit at the source. `tenant.city` becomes `'Medellín'` in `src/tenant/tenant.ts`, the ONLY place the studio's physical facts are written; C-05's `PageHead` sub, the customer `AuthShell` footer and the M-04 `EmailsPage` footer already interpolate `tenant.city`, so they corrected themselves and no screen code changed. `tenant.timezone` stays `'America/Bogota'`: that is Colombia's IANA zone identifier, not a statement about the studio's address, and changing it would have shifted every stored timestamp's display for no reason. Two hardcoded spellings were fixed: `README.md` line 3 ("a wellness club in Medellín, Colombia"), and the `EmailPreview` D-02 usage, whose footer literal read "HOY Wellness Center · Bogotá · …" — it now builds the string from `tenant.legalName` / `tenant.city` / `tenant.name`, matching what `EmailsPage` renders, so the library entry and the screen it documents cannot drift again. `tenant.contact.address` is still the placeholder "Dirección del estudio (pendiente)" and names no city, so it needed nothing. A grep of `src/` and `docs/pages/` found every remaining "Bogot" to be timezone wording ("all timestamps UTC with America/Bogota display" in `src/specs/canvasSpecs.ts`, `docs/pages/M-03.md`, `docs/screenshots/routes.json`; "hora de Bogotá" in the frozen ops manual) — correct as written and left untouched.
rejected: (1) Leaving `city: 'Bogotá'` and treating Jas's screenshot as a mock-up — Justin confirmed the studio is in Medellín, and the config is what the app renders, so the app would have kept showing a city the club is not in. (2) Changing `timezone` to something "Medellín"-shaped — there is no `America/Medellin` IANA zone; Colombia has one zone, `America/Bogota`, and renaming it would break date handling to fix a cosmetic misreading. (3) Editing the hardcoded "Bogotá" in `EmailPreview.meta.ts` to a hardcoded "Medellín" — that keeps a second source for a tenant fact and breaks the multi-tenant rule the same way, just with a different string. (4) Rewriting the ops-manual and M-03 timezone sentences to say Medellín — they describe UTC storage with `America/Bogota` display, which is true and is not about the studio's address. (5) A full `npm run screenshots` pass — only C-05 shows the city, so only C-05 was re-photographed.
files: src/tenant/tenant.ts src/components/organism/EmailPreview/EmailPreview.meta.ts README.md docs/screenshots/C-05/{es-390,es-1280,en-390,en-1280}.jpg docs/kanban.md docs/prompts/0015-tenant-city-medellin.md docs/changelog/0015-tenant-city-medellin.md
codes: C-05 (M-04 and A-01 footers follow from the tenant config; D-02 EmailPreview entry updated)

## Verification
- `npm run build`: zero TypeScript errors, `vite build` clean.
- `npm run screenshots -- --only=/app/payment-methods`: 1 route, no console errors; all four C-05 captures
  (ES/EN × 390/1280) changed and the subtitle reads "Medellín · COP".
- `grep -rn "Bogot" src/ docs/pages/ README.md`: only the timezone occurrences listed above remain.
