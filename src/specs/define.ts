import type { PageSpec } from './types';
/** Author a spec for a page that has no canvas code yet. All fields required so the inspector is never empty. */
export function defineSpec(spec: PageSpec): PageSpec { return spec; }
